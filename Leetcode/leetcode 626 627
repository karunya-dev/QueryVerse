
   Problem 626: Exchange Seats
   Medium
   
   Swap the seat id of every two consecutive students.
   If odd number of students, last one stays unchanged.
  

-- Solution:
SELECT 
    CASE
        WHEN id % 2 = 1 AND id + 1 <= (SELECT MAX(id) FROM Seat) 
            THEN id + 1
        WHEN id % 2 = 0 
            THEN id - 1
        ELSE id
    END AS id,
    student
FROM Seat
ORDER BY id;


   Problem 627: Swap Sex of Employees
   Easy
   
   Swap all 'm' and 'f' values in Salary table.
  

-- Solution:
UPDATE Salary
SET sex = CASE 
             WHEN sex = 'm' THEN 'f'
             ELSE 'm'
          END;

